server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.json;
    default_type application/json;

    location / {
        try_files $uri $uri/index.json $uri.json $uri/ =404;
    }
}
